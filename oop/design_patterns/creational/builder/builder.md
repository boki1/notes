Builder
----------

**Intent: Short version**
Separates the construction of a complex object from its representation (or instantiating) so that the same construction could be used for multiple and different representations.

**When to use?**
The algorithm for creating a complex object needs to be independent of 

